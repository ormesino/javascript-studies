{ // percorrendo o array e imprimindo os elementos e seus índices
	const num = [100, 200, 300, 400, 500, 600];
	for (let i = 0; i < num.length; i++) {
		console.log(`O número ${num[i]} está no índice ${i}`);
	}
}